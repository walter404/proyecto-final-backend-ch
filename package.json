{
  "name": "backend-entregafinal",
  "version": "1.0.0",
  "description": "Entrega Final del curso de Backend en CoderHouse",
  "main": "app.js",
  "scripts": {
    "build": "vite build",
    "start": "node server/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
